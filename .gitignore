target/
.git/
*.cache
float_scan_results.txt
.worktrees/
.claude/worktrees/
__pycache__/
*.pyc

# Local raw Criterion run archives (scripts/archive_criterion_run.sh).
# Large, regenerable build artifacts -- not source. Promote a summary to
# docs/PERFORMANCE_BASELINE_YYYY-MM-DD*.{md,json} via
# scripts/generate_performance_baseline.sh when a number needs to be
# committed and citable.
bench-archive/

# Coq build artifacts (regenerated by coqc; should never be tracked)
*.vo
*.vos
*.vok
*.glob
.*.aux

# Lean build artifacts
.lake/

# op_timings.rs JSON captures (crates/nine65/tests/op_timings.rs, issue #19).
# Per-run, per-machine measurement output -- not committed. The checked-in
# regression baseline lives at docs/benchmarks/op_timings_baseline.json.
/bench-results/
